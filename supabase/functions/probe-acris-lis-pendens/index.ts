// Decommissioned throwaway research probe (reused: ACRIS lis-pendens lookup,
// SimplyRETS coverage check, NJOGIS zip-field check). No delete-function
// tool is available in this session, so this is left as an inert stub.
// Safe to delete manually in the Supabase dashboard.
Deno.serve(() => new Response('decommissioned', { status: 410 }));
